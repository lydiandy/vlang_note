## 附录1 关键字

V语言目前一共有42个关键字:

```v
as
asm
assert
atomic
break
chan
const
continue
defer
else
enum
false
fn
for
go
goto
if
import
in
interface
is
lock
match
module
mut
none
or
pub
return
rlock
select
shared
sizeof
static
struct
thread
true
type
typeof
union
unsafe
```

